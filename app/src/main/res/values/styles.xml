<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:textViewStyle">@font/myriad_pro_regular</item>
        <item name="android:buttonStyle">@font/bebas_neue_bold</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="ButtonBack" parent="AppTheme">
        <item name="android:fontFamily">@font/bebas_neue_bold</item>
        <item name="android:textSize">24sp</item>
        <item name="android:layout_width">0dp</item>
        <item name="android:layout_height">45dp</item>
        <item name="android:layout_weight">1</item>
        <item name="android:textColor">@color/colorPrimary</item>
    </style>

    <style name="ButtonOrange" parent="ButtonBack">
        <item name="android:background">@drawable/button_orange_shape</item>
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="TextTitleDialog" parent="AppTheme">
        <item name="android:fontFamily">@font/bebas_neue_regular</item>
    </style>

    <style name="TextDialog" parent="AppTheme">
        <item name="android:fontFamily">@font/myriad_pro_regular</item>
    </style>

    <style name="MySpinnerLook" parent="Widget.AppCompat.Light.DropDownItem.Spinner">
        <item name="android:textSize">2sp</item>
    </style>
</resources>
